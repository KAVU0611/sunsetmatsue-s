{
  "name": "sunset-forecast",
  "private": true,
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "format": "pnpm -r format",
    "deploy:frontend": "bash ./scripts/deploy-frontend.sh"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
